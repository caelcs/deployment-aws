---
  - name: Deploy platform
    hosts: masters
    gather_facts: True
    become: true
    remote_user: ubuntu
    tasks:
      - name: Copy docker compose yml file to the image
        copy:
          src: apps.yml
          dest: /home/ubuntu
          follow: yes
      - name: Deploy Application
        command: docker-compose -f apps.yml up -d
